<%= content_tag(:div, :class => 'well') do %>
	<%= form_tag(route_for_search, {:method => :get,:class => '  form-search '}) do  %>
		<div class="input-append offset6">
  	      <%= text_field_tag(:search,nil,:class => 'span3 search-query',:size => 50, :placeholder => 'Faça sua busca')  %>
  	       <%= submit_tag('Busca', :class => 'btn btn-primary') %>
        </div>
        <div class="right">
		<%= link_to("<i class=\"icon-plus\"></i> #{title}".html_safe, route_for_new,:class => 'btn',:title => "Cadastre uma #{title}")  %>
	 </div>
	<% end  %>
	
<% end  %>

