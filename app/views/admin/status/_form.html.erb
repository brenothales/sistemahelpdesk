<%= simple_form_for [:admin, @statu] do |f| %>
     <%= error_message_for(@statu) %>

  <div class="form-inputs">
    <%= f.input :nome %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
